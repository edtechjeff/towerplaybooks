---
- name: to check WSUS server if patches are waiting to be approved
  hosts: "{{ ansible_host }}"
  gather_facts: false
  tasks:
  - name: Uninstall hotfix with KB
    win_shell: wusa /uninstall /kb:4577668 /quiet /norestart /log:c:\uninstall.log
