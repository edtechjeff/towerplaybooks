---
- hosts: "{{ ansible_play_batch }}"
  gather_facts: false
  tasks:
  - name: Checkin
    win_updates:
      category_names: 
        - SecurityUpdates
        - CriticalUpdates
      state: searched
